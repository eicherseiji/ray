group: runtime train tests
depends_on:
  - forge
  - mlbuild
  - ray-core-build
  - ray-dashboard-build
steps:
  - label: ":bullettrain_front: train: runtime tests"
    instance_type: medium
    commands:
      - bazel run //ci/ray_ci:test_in_docker -- //python/ray/anyscale/train/... ml
    tags:
      - ml
      - ml-turbo
