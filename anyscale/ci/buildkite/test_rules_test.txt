# Unit tests for config-premerge.yaml and config-postmerge.yaml test rules
#
# Run with:
#   RAYCI_TEST_RULE_FILES=anyscale/ci/buildkite/test_rules_always.txt,anyscale/ci/buildkite/test_rules.txt,ci/pipeline/test_rules.txt rayci test-rules anyscale/ci/buildkite/test_rules_test.txt
#
# Format: file_path: tag1 tag2 tag3
#
# NOTE: Rules are processed in order. The first file (test_rules_always.txt)
# adds "always lint" to all files via its wildcard fallthrough.

# === Anyscale Go directories ===
anyscale/go/main.go: always lint gotools
anyscale/go/cmd/wanda/main.go: always lint gotools

# === Anyscale ML ===
anyscale/ci/buildkite/ml.rayci.yml: always lint ml ml-turbo
python/ray/anyscale/train/trainer.py: always lint ml ml-turbo

# === Anyscale Serve ===
anyscale/ci/buildkite/serve.rayci.yml: always lint serve
python/ray/anyscale/serve/config.py: always lint serve

# === Anyscale Data ===
anyscale/ci/buildkite/data.rayci.yml: always lint data data-turbo ml ml-turbo train linux_wheels
python/ray/anyscale/data/dataset.py: always lint data data-turbo ml ml-turbo train linux_wheels

# === Anyscale Lineage ===
anyscale/ci/buildkite/lineage.rayci.yml: always lint lineage
python/ray/anyscale/lineage/tracker.py: always lint lineage

# === Anyscale Runtime ===
anyscale/ci/buildkite/anyscaleruntime.rayci.yml: always lint anyscaleruntime
anyscaleruntime/main.py: always lint anyscaleruntime

# === Anyscale Core ===
anyscale/ci/buildkite/core.rayci.yml: always lint python core-turbo linux_wheels
python/ray/anyscale/util.py: always lint python core-turbo linux_wheels

# === Anyscale Safetensors ===
anyscale/ci/buildkite/safetensors.rayci.yml: always lint safetensors
python/ray/anyscale/safetensors/loader.py: always lint safetensors

# === Anyscale fallback (other anyscale/ files) ===
anyscale/ci/config-premerge.yaml: always lint docker tools

# === OSS Ray Data ===
python/ray/data/dataset.py: always lint data ml train

# === OSS Ray Serve ===
python/ray/serve/api.py: always lint serve linux_wheels java

# === OSS Ray Tune ===
python/ray/tune/tuner.py: always lint ml train tune linux_wheels

# === OSS Ray Train ===
python/ray/train/trainer.py: always lint ml train linux_wheels

# === OSS Python core ===
python/ray/core.py: always lint ml tune train data python dashboard linux_wheels macos_wheels java

# === Skip rules (pass through without tags from ci/pipeline/test_rules.txt) ===
doc/source/index.rst: always lint doc
examples/readme.md: always lint
.github/workflows/ci.yml: always lint

# === Default fallback (unmatched files get all tags) ===
some/random/file.txt: always lint ml tune train data serve core_cpp cpp java python doc linux_wheels macos_wheels dashboard tools release_tests
