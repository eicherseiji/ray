group: runtime serve tests
steps:
  - label: ":ray-serve: serve: runtime tests"
    parallelism: 2
    instance_type: medium
    commands:
      - bazel run //ci/ray_ci:test_in_docker -- //python/ray/anyscale/serve/... serve
        --workers "$${BUILDKITE_PARALLEL_JOB_COUNT}" --worker-id "$${BUILDKITE_PARALLEL_JOB}" --parallelism-per-worker 3
        --except-tags direct_ingress
    depends_on: servebuild
    job_env: forge
    tags:
      - serve

  - label: ":ray-serve: serve: gRPC tests"
    parallelism: 2
    instance_type: large
    commands:
      - bazel run //ci/ray_ci:test_in_docker -- //python/ray/serve/... //python/ray/tests/...  serve
        --except-tags post_wheel_build,gpu,ha_integration
        --workers "$${BUILDKITE_PARALLEL_JOB_COUNT}" --worker-id "$${BUILDKITE_PARALLEL_JOB}" --parallelism-per-worker 3
        --build-name servebuild --test-env=ANYSCALE_RAY_SERVE_USE_GRPC_BY_DEFAULT=1
        --test-env=ANYSCALE_RAY_SERVE_GRPC_RUN_PROXY_ROUTER_SEPARATE_LOOP=0
        --test-env=ANYSCALE_RAY_SERVE_REPLICA_GRPC_MAX_MESSAGE_LENGTH=10500000
    depends_on: servebuild
    job_env: forge
    tags:
      - serve

  - label: ":ray-serve: serve: direct ingress tests"
    instance_type: large
    commands:
      - bazel run //ci/ray_ci:test_in_docker --
        //python/ray/serve/tests:test_api
        //python/ray/serve/tests:test_callback
        //python/ray/serve/tests:test_deploy
        //python/ray/serve/tests:test_deploy_app
        //python/ray/serve/tests:test_failure
        //python/ray/serve/tests:test_fastapi
        //python/ray/serve/tests:test_handle_cancellation
        //python/ray/serve/tests:test_metrics
        //python/ray/serve/tests:test_websockets
        //python/ray/serve/tests:test_request_timeout
        //python/ray/serve/tests:test_streaming_response
        //python/ray/serve/tests:test_callback
        //python/ray/anyscale/serve:tests/test_direct_ingress
        //python/ray/anyscale/serve:tests/test_tracing_utils
        serve
        --workers "$${BUILDKITE_PARALLEL_JOB_COUNT}" --worker-id "$${BUILDKITE_PARALLEL_JOB}" --parallelism-per-worker 3
        --build-name servebuild --test-env=ANYSCALE_RAY_SERVE_ENABLE_DIRECT_INGRESS=1
        --test-env=ANYSCALE_RAY_SERVE_DIRECT_INGRESS_MIN_DRAINING_PERIOD_S=0.01
    depends_on: servebuild
    job_env: forge
    tags:
      - serve
